apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mytardis.fullname" . }}-test-connection"
  labels:
    {{- include "mytardis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mytardis.fullname" . }}:{{ .Values.django.service.port }}']
  restartPolicy: Never
