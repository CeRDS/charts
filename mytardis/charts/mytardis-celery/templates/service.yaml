apiVersion: v1
kind: Service
metadata:
  name: {{ include "mytardis-celery.fullname" . }}
  labels:
    {{- include "mytardis-celery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mytardis-celery.selectorLabels" . | nindent 4 }}
